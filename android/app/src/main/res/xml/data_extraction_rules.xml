<?xml version="1.0" encoding="utf-8"?>
<!--
  Nothing leaves the device. The local vault holds identity private keys and
  session state; a cloud backup of it would put the one thing that must stay on
  the handset into someone else's storage.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
