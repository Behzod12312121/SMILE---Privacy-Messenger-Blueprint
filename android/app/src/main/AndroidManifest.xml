<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!--
      specialUse rather than dataSync. Android 14 forbids starting a dataSync
      foreground service from BOOT_COMPLETED outright, and the refusal arrives
      as an exception from startForeground — which, thrown while the service is
      being created, takes the whole app down. Seen on a handset: "FGS type
      dataSync not allowed to start from BOOT_COMPLETED", followed by a crash
      dialog. specialUse is permitted from boot and is an honest description:
      holding one connection open is the entire job.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.MillyGram"
        android:usesCleartextTraffic="false">

        <!--
          An empty taskAffinity puts this activity in a task no other app can
          name. Android groups activities by affinity, and the default affinity
          is the package name — which a hostile app can simply declare as its
          own, inserting itself into the task so its screen appears when the
          user returns to what they think is this app. That is the StrandHogg
          class of attack, and an empty affinity is what closes it.

          allowTaskReparenting is off for the same reason: it is the mechanism
          that would let this activity be moved into someone else's task.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:allowTaskReparenting="false"
            android:excludeFromRecents="false"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Holds the delivery socket open while the app is not in front. Not
          exported: nothing outside this app has any business starting or
          binding to it.
        -->
        <!--
          Restarts delivery after a reboot or an update, so a phone that
          restarted overnight is not silent until somebody opens the app.
        -->
        <receiver
            android:name=".BootReceiver"
            android:exported="false"
            android:directBootAware="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".DeliveryService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Holds the end-to-end encrypted delivery connection open so messages arrive while the app is closed. Push cannot be used without giving a third party a per-account device identifier." />
        </service>

        <!--
          Removed, not merged in. androidx.profileinstaller arrives through
          Compose and contributes an exported broadcast receiver plus its
          startup initialiser. Both exist so a build machine can push a
          baseline profile and so benchmarks can trigger recompilation; neither
          does anything for a user holding the app.

          What they do add is an exported component on a messenger whose whole
          claim is a small attack surface, and "exported receiver from a
          profiling library" is exactly the line an auditor stops on. Startup
          profiles still ship inside the APK and are still applied; only the
          remote trigger goes away.
        -->
        <receiver
            android:name="androidx.profileinstaller.ProfileInstallReceiver"
            tools:node="remove" />
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            tools:node="remove" />

    </application>
</manifest>
