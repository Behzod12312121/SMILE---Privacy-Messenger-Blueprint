<?xml version="1.0" encoding="utf-8"?>
<!--
  Instrumented tests only. 10.0.2.2 is the emulator's alias for the host
  loopback, where the gateway runs during an end-to-end run.

  Scoped to that one address rather than enabling cleartext generally: a test
  configuration that is broadly permissive has a way of being copied into a
  shipping one.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
